<!DOCTYPE html>
<html lang="uk">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="icon" type="image/png" href="/favicon.png">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="<%= (theme || 'light') %>-theme">
    <header class="header">
        <nav class="navbar">
            <div class="nav-left">
                <a href="/">Головна</a>
                <% if (user) { %>
                    <a href="/users">Користувачі</a>
                    <a href="/protected">Захищена сторінка</a>
                <% } %>
                <a href="/articles">Статті</a>
            </div>
            <div class="nav-right">
                <% if (user) { %>
                    <a href="/auth/profile"><%= user.username %></a>
                    <form class="inline-form" method="POST" action="/auth/logout">
                        <button class="logout-btn" type="submit">Вийти</button>
                    </form>
                <% } else { %>
                    <a href="/auth/login">Увійти</a>
                    <a href="/auth/register">Реєстрація</a>
                <% } %>
            </div>
        </nav>
    </header>
    
    <main class="main-content">
        <div class="error-page">
            <h1 class="error-title">Стаття не знайдена</h1>
            <p class="error-message">Вибачте, але стаття з таким ID не існує.</p>
            
            <div class="action-buttons">
                <a href="/articles" class="btn btn-primary">← Назад до списку статей</a>
                <a href="/" class="btn btn-secondary">На головну</a>
            </div>
        </div>
    </main>
    
    <footer class="footer">
        <p>&copy; 2025 Express App з PUG та EJS</p>
    </footer>
</body>
</html>